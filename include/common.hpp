#pragma once

#include <vector>
#include <iostream>
#include <string>

const int MAX_CARDS = 52;
const int SUIT_COUNT = 4;
const int MAX_VALUE = 13;

enum Suit
{
    DIAMONDS,
    CLUBS,
    HEARTS,
    SPADES
};